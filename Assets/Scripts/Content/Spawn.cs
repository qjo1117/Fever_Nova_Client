using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// 이거 설계는 좀 더 물어보고 할 생각
// 기획자 쪽에서 일일히 배치한다는 소리를 해서 맞춰서 제작해야할 것 같음

public class Spawn : MonoBehaviour
{
    // 이건 컴포넌트가 아닌 자료로 따로 가지고 있어야한다고 생각한다.
    private MonsterController m_monster = null;


    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
